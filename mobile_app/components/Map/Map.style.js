import { StyleSheet } from 'react-native';

const s = StyleSheet.create({
    mapContainer: {
      flex : 1,
      backgroundColor: '#f0f0f0',
      borderWidth: 1,
      borderColor: '#ccc',
      borderRadius: 10,
      overflow: 'hidden', 
      margin: 20,
    },
  });

  export { s }
  